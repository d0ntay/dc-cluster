apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: batfish-data
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: {{ .Values.batfish.storage }}
